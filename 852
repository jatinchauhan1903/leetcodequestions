class Solution {
    public int peakIndexInMountainArray(int[] arr) {
        int x=arr[0],y=0;
        for(int i = 0;i<arr.length;i++)
        {
            if(arr[i]>x)
            {
                x=arr[i];
                y=i;
            }
        }return y;
        
    }
}
